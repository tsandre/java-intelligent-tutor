Thread t = new Thread(new Runnable()    
   public void run() {
                  
      for (int i = 0;
 i < 100000;
 i++) {

         if (stop) break;

         tfCount.setText("" + countValue);

         countValue++;

         try {

            Thread.sleep(10);

         }
 catch (InterruptedException ex) {
}

      }

   }

}
);

t.start();
